org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Required for flutter_local_notifications scheduled notifications in release APK (R8/ProGuard).
# If notifications still don't fire in release, this compatibility mode helps.
android.enableR8.fullMode=false
